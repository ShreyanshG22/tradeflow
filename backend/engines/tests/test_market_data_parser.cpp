#include <gtest/gtest.h>
// Main test file for market data parser

TEST(MarketDataParserTest, BasicTest) {
    EXPECT_TRUE(true);
}

int main(int argc, char **argv) {
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}